VITE_PLEROMA_INSTANCE_URL=https://social.luizpicolo.com.br

# API do serviço REA.fed que integra SHA-256, assinatura, OpenTimestamps e IPFS.
# Em produção pode ser um domínio separado; em um reverse proxy same-origin, use /api.
VITE_REA_IPFS_API_URL=http://localhost:8787/api

# Gateway usado pelo catálogo para abrir CIDs publicados no Pleroma.
VITE_IPFS_GATEWAY_URL=https://ipfs.io
